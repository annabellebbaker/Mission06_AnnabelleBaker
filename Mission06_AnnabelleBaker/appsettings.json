{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        "MoviesConnection": "Data Source = Movies.sqlite" // name of database
        // list of different sources I'm pulling from (key that points towards data source) - needs to connect to Program.cs name
    }
}
